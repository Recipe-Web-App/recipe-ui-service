---
# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: recipe-ui-service
  namespace: recipe-ui
  labels:
    app: recipe-ui-service
spec:
  selector:
    app: recipe-ui-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
